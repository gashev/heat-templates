#cloud-config

users:
  - default
  - name: cloud
    ssh-authorized-keys:
      - $ssh_key_1
      - $ssh_key_2
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    shell: /bin/bash

runcmd:
  - curl -X PUT http://$master_ip:2379/v2/keys/ansible/nodes/$(hostname) -d value="$ip"
